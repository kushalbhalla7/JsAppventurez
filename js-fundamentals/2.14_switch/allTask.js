"use strict"

//All task of switch

//Task 1: Rewrite the "switch"  into an "if"

// let brower = "Opera";

// if(brower === "Edge") {
//     console.log( "You've got the Edge!" );
// } else if(brower === 'Chrome') {
    
// } else if(brower === 'Firefox') {
    
// } else if(brower === 'Safari') {
    
// } else if(brower === 'Opera') {
//     console.log("Okay we support these browers too");
// } else {
//     console.log("we hope that this page looks ok!");
// }


//Task 2: Rewrite "if" inot "switch"

let a = +prompt('a?', '');

klsjfsa;
switch (a) {
    case 0: {
        console.log(0);
        break;
    }
    case 1: {
        console.log(a);
        break;
    }
    case 2 || 3: {
        console.log('2,3')
        break;
    }
    default:
      console.log( 'We hope that this page looks ok!' );
}